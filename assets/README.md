# Asset Placeholders

Add diagrams and screenshots used in technical docs.

Suggested files:
- `runner-pipeline.png` - stage diagram for executor/metric/aggregator
- `run-diff.png` - baseline vs candidate delta view
- `ci-gate.png` - example failing threshold in CI output

Do not include customer-identifiable traces.
